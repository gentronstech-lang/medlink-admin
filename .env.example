# Local dev (backend on your machine):
VITE_API_BASE_URL=http://localhost:3000
VITE_IMAGES_BASE_URL=http://localhost:3000

# Production (set in Vercel → Environment Variables, then redeploy):
# VITE_API_BASE_URL=https://api.medlink-africa.com
# VITE_IMAGES_BASE_URL=https://api.medlink-africa.com
VITE_GOOGLE_MAPS_API_KEY=your-google-maps-api-key

# Admin JWT: Set via localStorage after login
# localStorage.setItem('access_token', 'your-jwt-token')
